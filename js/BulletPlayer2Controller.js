class BulletPlayer2Controller extends BulletController {
    constructor(position, direction, angle) {
        super(position, direction, bulletPlayer2Group, 'bullet', angle);
    }
    update() {
        super.update();
    }
}
