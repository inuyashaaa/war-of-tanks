class Player1Controller extends PlayerController {
    constructor(x, y, configs, game) {
        super(x, y, configs, 'player1', player1Group, game);
    }
    update() {
      super.update();
    }
}
